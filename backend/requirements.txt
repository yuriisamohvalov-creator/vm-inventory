Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
psycopg2-binary==2.9.9
python-dotenv==1.0.0
WeasyPrint==60.1
pandas==2.1.3
openpyxl==3.1.2
drf-yasg==1.21.7
PyYAML==6.0.1
coreapi==2.3.3
setuptools==69.0.3  # Явно добавляем setuptools
wheel==0.42.0       # Добавляем wheel для сборки
gunicorn==21.2.0    # Добавляем gunicorn явно
# LDAP (optional): uncomment and install system deps in Dockerfile (libldap2-dev libsasl2-dev) when needed
# python-ldap>=3.4
