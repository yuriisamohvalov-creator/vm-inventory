apiVersion: v1
kind: Secret
metadata:
  name: nginx-ssl
type: kubernetes.io/tls
data:
  # SSL certificate and key (base64 encoded)
  # tls.crt: <base64-encoded-certificate>
  # tls.key: <base64-encoded-private-key>
  # Example (replace with actual certificates):
